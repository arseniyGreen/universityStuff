1)Массив ~ вектор
    Одинаковый тип данных <=> одинаковый размер памяти под 1 элемент
    Непрерывность

Долгие операции:
    Линейный поиск
    Добавление/удаление в/из середины

2)Связанный список (list)
    list<int> l;
    Используется, если необходимо часто делать вставки/удаление

Итераторы
    list<int>::iterator it = l.begin();
    while(it != l.end())
    {
        std::cout << *it << " ";
        *it++;
    }
